# Full platform stack (apply after operators are installed)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - bootstrap
  - ingress
  - platform-data
  - platform-tools
  - observability
