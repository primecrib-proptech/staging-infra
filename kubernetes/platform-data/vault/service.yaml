apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: platform-data
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8200
      targetPort: 8200
  selector:
    app.kubernetes.io/name: vault
    component: server
