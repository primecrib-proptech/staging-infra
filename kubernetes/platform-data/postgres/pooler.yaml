apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: proptech-pg-pooler
  namespace: platform-data
spec:
  cluster:
    name: proptech-pg
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "25"
